<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.qf.ddshop.dao.TbItemCustomMapper" >


  <sql id="Base_Column_List" >
    i.id, i.title, i.sell_point as sellPoint, i.price,
    i.num, i.barcode, i.image, i.cid, i.status, i.created, i.updated,
    c.name as catName
  </sql>

  <select id="countItems" resultType="_int">
    select count(1)
    from tb_item
    where status != 3
  </select>

  <!--在对应的dao接口里面，选择类名，选择Copy Reference,之后粘贴到这里，结果是权限定名-->
  <select id="listItemsByPage"  resultType="com.qf.ddshop.pojo.vo.TbItemCustom">
    SELECT
    <include refid="Base_Column_List" />
    FROM tb_item i
    LEFT JOIN tb_item_cat c ON i.cid = c.id
    where i.status != 3
    <choose>
      <when test="order != null and order.sort != null and order.order != null">
--         中文排序解决
        ORDER BY  CONVERT(i.${order.sort} USING gbk)  ${order.order}
      </when>
      <otherwise>
        ORDER BY i.id DESC
      </otherwise>
    </choose>
    LIMIT #{page.offset},#{page.rows}
  </select>
</mapper>